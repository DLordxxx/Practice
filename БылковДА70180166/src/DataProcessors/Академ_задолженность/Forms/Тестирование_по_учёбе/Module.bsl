
&НаКлиенте
Процедура Начать_тестирование(Команда)
	
	
	Элементы.Начать_тестирование.Видимость = Ложь;
	
	Элементы.Для_первого_вопроса.Видимость = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос1ответ1ПриИзменении(Элемент)
	
  Если ЭтаФорма.Вопрос1ответ2 = Истина или ЭтаФорма.Вопрос1ответ3 = Истина или ЭтаФорма.Вопрос1ответ4 = Истина Тогда
    ЭтаФорма.Вопрос1ответ1 = Ложь;
  КонецЕсли;  	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос1ответ2ПриИзменении(Элемент)
	
	Если ЭтаФорма.Вопрос1ответ1 = Истина или ЭтаФорма.Вопрос1ответ3 = Истина или ЭтаФорма.Вопрос1ответ4 = Истина Тогда
		ЭтаФорма.Вопрос1ответ2 = Ложь;
  КонецЕсли;  	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос1ответ3ПриИзменении(Элемент)
	
	Если ЭтаФорма.Вопрос1ответ2 = Истина или ЭтаФорма.Вопрос1ответ1 = Истина или ЭтаФорма.Вопрос1ответ4 = Истина Тогда
    ЭтаФорма.Вопрос1ответ3 = Ложь;
  КонецЕсли;  	
	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос1Ответ4ПриИзменении(Элемент)

  Если ЭтаФорма.Вопрос1ответ2 = Истина или ЭтаФорма.Вопрос1ответ3 = Истина или ЭтаФорма.Вопрос1ответ1 = Истина Тогда
    ЭтаФорма.Вопрос1ответ4 = Ложь;
  КонецЕсли;  


КонецПроцедуры




&НаКлиенте
Процедура Второй_вопрос(Команда)
	
	Если ЭтаФорма.Вопрос1ответ1 = Истина или ЭтаФорма.Вопрос1ответ2 = Истина или ЭтаФорма.Вопрос1ответ3 = Истина или ЭтаФорма.Вопрос1ответ4 = Истина Тогда	
	
		Элементы.Для_первого_вопроса.Видимость = Ложь;
		
		Элементы.Для_второго_вопроса.Видимость = Истина;    
		
		Если ЭтаФорма.Вопрос1ответ1 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок + Элементы.Вопрос1ответ1.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос1ответ2 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок+Элементы.Вопрос1ответ2.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос1ответ3 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок+Элементы.Вопрос1ответ3.Заголовок;
			
		КонецЕсли;    
		
		Если ЭтаФорма.Вопрос1ответ4 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок+Элементы.Вопрос1ответ4.Заголовок;
			
		КонецЕсли;    

		
	КонецЕсли;

	
КонецПроцедуры

&НаКлиенте
Процедура Третий_вопрос(Команда)
	
	Если ЭтаФорма.Вопрос2ответ1 = Истина или ЭтаФорма.Вопрос2ответ2 = Истина или ЭтаФорма.Вопрос2ответ3 = Истина или ЭтаФорма.Вопрос2ответ4 = Истина или ЭтаФорма.Вопрос2ответ5 = Истина Тогда
	
		Элементы.Для_второго_вопроса.Видимость = Ложь;        
		
		Элементы.Для_третьего_вопроса.Видимость = Истина;

		
		// Создаем переменную для хранения всех выбранных ответов
		ТекстОтветов = "";
		ПервыйОтвет = Истина;

		// Проверяем каждый ответ и добавляем его при необходимости
		Если ЭтаФорма.Вопрос2ответ1 = Истина Тогда
		    ТекстОтветов = Элементы.Вопрос2ответ1.Заголовок;
		    ПервыйОтвет = Ложь;
		КонецЕсли;

		Если ЭтаФорма.Вопрос2ответ2 = Истина Тогда
		    Если НЕ ПервыйОтвет Тогда
		        ТекстОтветов = ТекстОтветов + ", ";
		    КонецЕсли;
		    ТекстОтветов = ТекстОтветов + Элементы.Вопрос2ответ2.Заголовок;
		    ПервыйОтвет = Ложь;
		КонецЕсли;

		Если ЭтаФорма.Вопрос2ответ3 = Истина Тогда
		    Если НЕ ПервыйОтвет Тогда
		        ТекстОтветов = ТекстОтветов + ", ";
		    КонецЕсли;
		    ТекстОтветов = ТекстОтветов + Элементы.Вопрос2ответ3.Заголовок;
		    ПервыйОтвет = Ложь;
		КонецЕсли;

		Если ЭтаФорма.Вопрос2ответ4 = Истина Тогда
		    Если НЕ ПервыйОтвет Тогда
		        ТекстОтветов = ТекстОтветов + ", ";
		    КонецЕсли;
		    ТекстОтветов = ТекстОтветов + Элементы.Вопрос2ответ4.Заголовок;
		    ПервыйОтвет = Ложь;
		КонецЕсли;

		Если ЭтаФорма.Вопрос2ответ5 = Истина Тогда
		    Если НЕ ПервыйОтвет Тогда
		        ТекстОтветов = ТекстОтветов + ", ";
		    КонецЕсли;
		    ТекстОтветов = ТекстОтветов + Элементы.Вопрос2ответ5.Заголовок;
		КонецЕсли;

		// Присваиваем собранный текст заголовку
		Элементы.Выбранный_вами_ответ_на_второй_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_второй_вопрос.Заголовок+ТекстОтветов;
					
	КонецЕсли;
	
	
КонецПроцедуры

&НаКлиенте
Процедура Четвёртый_вопрос(Команда)
	
	Если ЭтаФорма.Вопрос3ответ1 = Истина или ЭтаФорма.Вопрос3ответ2 = Истина или ЭтаФорма.Вопрос3ответ3 = Истина Тогда	
	
		Элементы.Для_третьего_вопроса.Видимость = Ложь;
		
		Элементы.Для_четвёртого_вопроса.Видимость = Истина;    
		
		Если ЭтаФорма.Вопрос3ответ1 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок+Элементы.Вопрос3ответ1.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос3ответ2 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок+Элементы.Вопрос3ответ2.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос3ответ3 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок+Элементы.Вопрос3ответ3.Заголовок;
			
		КонецЕсли;    
		
	КонецЕсли;
	
	
	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос3ответ1ПриИзменении(Элемент)

   Если ЭтаФорма.Вопрос3ответ2 = Истина или ЭтаФорма.Вопрос3ответ3 Тогда
    ЭтаФорма.Вопрос3ответ1 = Ложь;
КонецЕсли;  	


КонецПроцедуры

&НаКлиенте
Процедура Вопрос3ответ2ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос3ответ1 = Истина или ЭтаФорма.Вопрос3ответ3 Тогда
    ЭтаФорма.Вопрос3ответ2 = Ложь;
КонецЕсли; 	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос3ответ3ПриИзменении(Элемент)
	

   Если ЭтаФорма.Вопрос3ответ2 = Истина или ЭтаФорма.Вопрос3ответ1 Тогда
    ЭтаФорма.Вопрос3ответ3 = Ложь;
	КонецЕсли; 
	
	
КонецПроцедуры

&НаКлиенте
Процедура Пятый_вопрос(Команда)
	
	Если ЭтаФорма.Вопрос4ответ1 = Истина или ЭтаФорма.Вопрос4ответ2 = Истина или ЭтаФорма.Вопрос4ответ3 = Истина Тогда	
	
		Элементы.Для_четвёртого_вопроса.Видимость = Ложь;
		
		Элементы.Для_пятого_вопроса.Видимость = Истина;    
		
		Если ЭтаФорма.Вопрос4ответ1 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок+Элементы.Вопрос4ответ1.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос4ответ2 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок =Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок+ Элементы.Вопрос4ответ2.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос4ответ3 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок+Элементы.Вопрос4ответ3.Заголовок;
			
		КонецЕсли;    
		
	КонецЕсли;
	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос4ответ1ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос4ответ2 = Истина или ЭтаФорма.Вопрос4ответ3 Тогда
    ЭтаФорма.Вопрос4ответ1 = Ложь;
КонецЕсли;  	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос4ответ2ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос4ответ1 = Истина или ЭтаФорма.Вопрос4ответ3 Тогда
    ЭтаФорма.Вопрос4ответ2 = Ложь;
КонецЕсли;  	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос4ответ3ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос4ответ2 = Истина или ЭтаФорма.Вопрос4ответ1 Тогда
    ЭтаФорма.Вопрос4ответ3 = Ложь;
КонецЕсли;  	
	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Завершение(Команда)
	
	Если ЭтаФорма.Вопрос5ответ1 = Истина или ЭтаФорма.Вопрос5ответ2 = Истина или ЭтаФорма.Вопрос5ответ3 = Истина Тогда	
	
		Элементы.Для_пятого_вопроса.Видимость = Ложь;
		
		//Элементы.Сохранение_результатов.Видимость = Истина;    
		
		Если ЭтаФорма.Вопрос5ответ1 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок+Элементы.Вопрос5ответ1.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос5ответ2 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок =Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок+ Элементы.Вопрос5ответ2.Заголовок;
			
		КонецЕсли;

		Если ЭтаФорма.Вопрос5ответ3 = Истина Тогда
			
			Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок = Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок+Элементы.Вопрос5ответ3.Заголовок;
			
		КонецЕсли;    
		
	КонецЕсли;  
	
	Если Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = "1. Теоретические основы" и Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Часто, что создает трудности в подготовке" и Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. Я сталкиваюсь с значительными трудностями при попытках применить теоретические знания на практике"  Тогда
		
		Элементы.Показ_ответов.Видимость = Истина;   
		
		Элементы.Сохранение_результатов.Видимость = Истина;
		
	КонецЕсли;   
	
	Если Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = "1. Практические задания" и (Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Иногда, но в целом я способен найти необходимые ресурсы" или Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Часто, что создает трудности в подготовке") и (Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. У меня имеются некоторые трудности, однако я прилагаю усилия для улучшения своих навыков" или Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. Я сталкиваюсь с значительными трудностями при попытках применить теоретические знания на практике")  Тогда
		
		Элементы.Показ_ответов_2.Видимость = Истина;   
		
		Элементы.Сохранение_результатов.Видимость = Истина;
		
	КонецЕсли;

	Если Элементы.Выбранный_вами_ответ_на_первый_вопрос.Заголовок = "1. Подготовка к сессии" и (Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Иногда, но в целом я способен найти необходимые ресурсы" или Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Часто, что создает трудности в подготовке") и Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. У меня имеются некоторые трудности, однако я прилагаю усилия для улучшения своих навыков"  и Элементы.Выбранный_вами_ответ_на_пятый_вопрос.Заголовок = "5. Имеет первостепенное значение для моего успешного обучения" Тогда
		
		Элементы.Показ_ответов_3.Видимость = Истина;   
		
		Элементы.Сохранение_результатов.Видимость = Истина;
		
	КонецЕсли;  	
	
	Если Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Никогда, у меня есть все необходимое"  и (Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. У меня имеются некоторые трудности, однако я прилагаю усилия для улучшения своих навыков" или Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. Я сталкиваюсь с значительными трудностями при попытках применить теоретические знания на практике")  Тогда
		
		Элементы.Показ_ответов_4.Видимость = Истина;   
		
		Элементы.Сохранение_результатов.Видимость = Истина;
		
	КонецЕсли;
	
	Если Элементы.Выбранный_вами_ответ_на_третий_вопрос.Заголовок =  "3. Никогда, у меня есть все необходимое" и Элементы.Выбранный_вами_ответ_на_четвёртый_вопрос.Заголовок = "4. Я уверенно применяю теоретические знания в практических ситуациях"  Тогда
		
		Элементы.Показ_ответов_5.Видимость = Истина;   
		
		Элементы.Сохранение_результатов.Видимость = Истина;
		
	КонецЕсли;   	
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос5ответ1ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос5ответ2 = Истина или ЭтаФорма.Вопрос5ответ3 Тогда
    ЭтаФорма.Вопрос5ответ1 = Ложь;
КонецЕсли;  	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос5ответ2ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос5ответ1 = Истина или ЭтаФорма.Вопрос5ответ3 Тогда
    ЭтаФорма.Вопрос5ответ2 = Ложь;
КонецЕсли;  	
	
	
КонецПроцедуры

&НаКлиенте
Процедура Вопрос5ответ3ПриИзменении(Элемент)
	
   Если ЭтаФорма.Вопрос5ответ2 = Истина или ЭтаФорма.Вопрос5ответ1 Тогда
    ЭтаФорма.Вопрос5ответ3 = Ложь;
КонецЕсли;  	
	
	
КонецПроцедуры


















