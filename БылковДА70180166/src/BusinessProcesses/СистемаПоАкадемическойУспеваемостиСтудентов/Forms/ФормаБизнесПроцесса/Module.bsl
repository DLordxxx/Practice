
&НаСервере
Процедура ПоказатьКартуНаСервере()
	Карта = Объект.Ссылка.ПолучитьОбъект().ПолучитьКартуМаршрута();
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьКарту(Команда)
	ПоказатьКартуНаСервере();
КонецПроцедуры
